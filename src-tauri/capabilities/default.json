{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacidades por defecto para la app",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
